// get search box filed to get search anything 
const inputBox = document.getElementById('search-box');

// get search button
const searchBtn = document.getElementById('search-btn');

// img container that shows anything on the page
const imgConatiner = document.getElementById('img-container');

// show more result button
const showMoreBtn = document.getElementById('showMore-btn');
